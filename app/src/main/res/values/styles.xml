<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="Transparent" parent="android:Theme">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>

    <style name="FullscreenTheme" parent="android:Theme.NoTitleBar">
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowBackground">@null</item>
        <item name="metaButtonBarStyle">@style/ButtonBar</item>
        <item name="metaButtonBarButtonStyle">@style/ButtonBarButton</item>
    </style>

    <style name="ButtonBar">
        <item name="android:paddingLeft">2dp</item>
        <item name="android:paddingTop">5dp</item>
        <item name="android:paddingRight">2dp</item>
        <item name="android:paddingBottom">0dp</item>
        <item name="android:background">@color/black_overlay</item>
    </style>

    <style name="rectangleImageView">
        <item name="android:padding">2dp</item>
        <item name="android:layout_margin">2dp</item>
    </style>

    <style name="roundImageView">
    </style>

    <style name="ButtonBarButton">
        <item name="android:background">@color/backgroundBlueOverlay</item>
        <item name="android:padding">10dp</item>
        <item name="android:layout_marginTop">2dp</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="StandardText" parent="@android:style/TextAppearance.Medium">
        <item name="android:typeface">normal</item>
        <item name="android:padding">2dp</item>
        <item name="android:textSize">30sp</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="won_game_string" parent="StandardText">
        <item name="android:textColor">@color/green</item>
    </style>

    <style name="have_enough" parent="StandardText">
        <item name="android:textColor">@color/red</item>
        <item name="android:textSize">20sp</item>
    </style>

    <style name="game_over_string" parent="StandardText">
        <item name="android:textColor">@color/red</item>
    </style>

</resources>
